Explicación

- 1: Esta línea lo que hace es exportar eserSchema y transformarlo para que sea reutilizable en todo el proyecto, al agregar mongoose.model hacemos que nuestro userSchema tenga nuevas funciones de mongo como .find(), .save(), etc. Toma los parámetros de userShcema y lo renombramos User para que se guarden todos los valores en una variable. A parte crea una carpeta automáticamente en MongoDB que le hace cambios como ponerle una s para que sea plural y en lowercase, que ocasiona que ya no sea User si no users, esto también lo haría si en vez de User lo llamamos Usuario entonce se llamaria usuarios.  